export type Song = {
    id: string;
    artistId: string;
    title: string;
    slug: string;
}

export type SongVariant = {
    id: string;
    songId: string;
    title: string;
    slug: string;
}